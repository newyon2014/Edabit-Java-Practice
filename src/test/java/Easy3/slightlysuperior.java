package Easy3;

public class slightlysuperior {

    /*public static boolean isFirstSuperior(Object a[], Object b[]) {
        boolean isSuperior = false;
        String.class instanceof a;


        for (int i = 0; i < a.length && i< b.length; i++) {
            if(a[i].getClass().equals(String.class) && b[i].getClass().equals(String.class)){
                if(a[i].toString().compareTo(b[i]).)
            }

        }

    }*/
}
